<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>fxy66</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- 配置基础样式 -->
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#2563eb',
                    },
                }
            }
        }
    </script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center p-4">
    <!-- 内容容器 -->
    <div class="max-w-md w-full bg-white rounded-xl shadow-sm p-6 md:p-8 text-center">
        <h1 class="text-2xl md:text-3xl font-bold text-gray-800 mb-4">正在加载</h1>
        <p class="text-gray-600 mb-6">
            付翔宇这辈子有了
        </p>
        <!-- 跳转倒计时提示 -->
        <p class="text-primary font-medium">
            页面将在 <span id="countdown">2</span> 秒后自动跳转至目标网站...
        </p>
        <!-- 手动跳转链接（备用） -->
        <p class="mt-4 text-sm text-gray-500">
            若未自动跳转，可点击 <a href="https://github.com" class="text-primary underline" target="_blank">此处</a> 手动访问
        </p>
    </div>

    <!-- 自动跳转脚本 -->
    <script>
        // 1. 设置跳转目标（替换为你需要的网站URL）
        const targetUrl = "https://github.com";
        // 2. 设置跳转延迟（单位：毫秒，2000ms = 2秒）
        const delay = 2000;

        // 可选：倒计时数字动态变化（增强用户感知）
        let countdown = 2;
        const countdownElement = document.getElementById('countdown');
        const timer = setInterval(() => {
            countdown--;
            countdownElement.textContent = countdown;
            if (countdown <= 0) clearInterval(timer);
        }, 1000);

        // 核心：延迟后自动跳转
        setTimeout(() => {
            window.location.href = targetUrl;
        }, delay);
    </script>
</body>
</html>
